export const selectNotices = (state) => state.notices.notices ;
export const selectNoticesId = (state) => state.notices.notices;
export const selectNoticesError = (state) => state.notices.error;
export const selectNoticesCategories = (state) => state.notices.categories;
export const selectNoticesSexOptions = (state) => state.notices.sexOptions;
export const selectNoticesSpeciesOptions = (state) => state.notices.speciesOptions;
export const selectNoticesCities = (state) => state.notices.cities;